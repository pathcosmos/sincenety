# Plan 4: Cloudflare D1 Central Database Sync

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Enable multi-machine data aggregation via Cloudflare D1 central database. `sincenety sync` pushes local data to D1, pulls shared config. Auto-sync after `out`.

**Architecture:** Local-first with D1 as aggregation layer. D1 REST API direct access (no Worker proxy). Machine ID for conflict resolution. Shared config for SMTP settings across machines.

**Tech Stack:** TypeScript ESM, Node 18+ fetch (zero new deps), Cloudflare D1 REST API

**Spec:** `/Users/lanco/.claude/plans/crystalline-crafting-hamster.md`

---

## Task 1: D1 Client

**Files:** Create `src/cloud/d1-client.ts`, Create `tests/d1-client.test.ts`

Implement `D1Client` class wrapping Cloudflare D1 REST API with `query()`, `batch()`, `ping()` methods. Use native `fetch`. Test with vi.fn() mock.

## Task 2: D1 Schema

**Files:** Create `src/cloud/d1-schema.ts`

D1 tables mirroring local schema + `machine_id` + `synced_at` columns. `shared_config` and `sync_meta` tables. `ensureD1Schema(client)` function.

## Task 3: Sync Engine

**Files:** Create `src/cloud/sync.ts`, Create `tests/sync.test.ts`

`pushToD1()`, `pullConfigFromD1()`, `getSyncStatus()`. Incremental sync via `last_d1_sync` timestamp. Batch upsert with ON CONFLICT.

## Task 4: CLI sync Command + Config D1 Options

**Files:** Modify `src/cli.ts`, Modify `src/config/setup-wizard.ts`

`sync` command (push/pull-config/status/init). Config D1 options (--d1-account, --d1-database, --d1-token, --machine-name, --setup-d1). Config status table D1 section.

## Task 5: Auto-sync in out + Integration

**Files:** Modify `src/core/out.ts`, Update READMEs

Auto-sync hook at end of `runOut()`. Non-fatal on failure. README updates. Full test suite.
