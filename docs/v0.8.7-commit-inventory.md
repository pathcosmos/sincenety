# v0.8.7 커밋 파일 인벤토리

> 커밋: `082c79e` (2026-04-16)
> 31 files changed, 3127 insertions(+), 100 deletions(-)

---

## 1. 핵심 버그 수정

v0.8.7의 주요 목적: `autoSummarize`가 당일 새 세션 추가 시 재요약하지 않던 버그 수정.

| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `src/core/circle.ts` | 수정 | `autoSummarize` — `summaryJson` 존재 체크를 freshness 기반 stale 재요약으로 교체. `runCircle`에 `rerun`/`needsSkillCommand` 옵션 추가, `allChanged` 병합 로직 |
| `package.json` | 수정 | 버전 0.8.6 → 0.8.7 |
| `src/cli.ts` | 수정 | `.version("0.8.7")`, `--rerun`/`--verify`/`--project-weight` 옵션, `doctor` 명령, `displayWidth` 유틸 분리 |

---

## 2. 신규 기능 소스

| 파일 | 기능 | 설명 |
|------|------|------|
| `src/core/doctor.ts` | `sincenety doctor` 명령 | 최근 N일간 요약 누락/stale/정상 상태 진단 테이블 출력 |
| `src/core/weights.ts` | 프로젝트별 중요도 가중치 | `high`/`normal`/`low` 레벨 설정, 이메일 렌더링 시 상세도 조절 |
| `src/util/display-width.ts` | 유니코드 표시 폭 유틸 | 한글/이모지 폭 계산 (`displayWidth`, `padEndW`), cli.ts에서 분리 |
| `src/core/ai-provider.ts` | 수정 | `assertAiReadyForCliPipeline`에 `needsSkillCommand` 파라미터 추가 |
| `src/core/out.ts` | 수정 | `--verify` 플래그, `collectUnrecordedSummaryErrors`, `printVerifyTable`, freshness 경고 |
| `src/storage/adapter.ts` | 수정 | `FreshnessInfo` 타입, `getDailyReportFreshness`/`invalidateDailyReport` 인터페이스 추가 |
| `src/storage/sqljs-adapter.ts` | 수정 | 위 인터페이스 구현 |
| `src/email/renderer.ts` | 수정 | AI provider 배지, 프로젝트 가중치별 렌더링 분기 |
| `src/email/template.ts` | 수정 | 출처 배지 HTML 섹션 추가 |
| `src/report/terminal.ts` | 수정 | `displayWidth`/`padEndW` 유틸 사용으로 전환 |
| `src/skill/SKILL.md` | 수정 | `--rerun`, `--verify` 관련 skill 가이드 업데이트 |

---

## 3. Hook 스크립트

| 파일 | 설명 |
|------|------|
| `hooks/guard-out.sh` | PreToolUse hook — `sincenety out`/`outd`/`outw`/`outm` 실행 전 `--verify`로 요약 상태 체크, MISSING/STALE이면 exit 2로 차단 |
| `hooks/README.md` | hook 설치/사용법 문서 |

---

## 4. 테스트 소스

### 4-1. 신규 테스트 파일 (5개)

| 파일 | 테스트 수 | 대상 | 테스트 기법 |
|------|----------|------|-----------|
| `tests/freshness.test.ts` | 16 | `getDailyReportFreshness`, `invalidateDailyReport` | 실제 SqlJsAdapter + tmpdir integration test. stale/fresh/emailed 경계값, v3 레거시 `updatedAt=null`, `emailedAt=0` falsy 비대칭 검증 |
| `tests/ai-provider.test.ts` | 15 | `detectAiProvider`, `assertAiReadyForCliPipeline`, `resolveAiProvider` | Mock storage + `process.exit`/`process.stdout.write` spy. 8가지 provider 감지 경로, ANTHROPIC_API_KEY env 우선순위, `claude-code` → needs_skill JSON exit 검증 |
| `tests/doctor.test.ts` | 14 | `runDoctor`, `printDoctorTable` | Mock storage + `vi.useFakeTimers()` (2026-04-16 고정). NO_DATA/OK/MISSING_SUMMARY/EMPTY_SUMMARY/STALE 5가지 상태, 아이콘 매핑, `--rerun` 제안 메시지 검증 |
| `tests/weights.test.ts` | 22 | `isWeightLevel`, `resolveWeight`, `getProjectWeights`, `setProjectWeight` | 순수 함수 직접 테스트 + mock storage. 타입 가드, basename fallback, trailing slash, 잘못된 JSON, non-object JSON 경계값 |
| `tests/guard-hook.test.ts` | 7 | `hooks/guard-out.sh` | `child_process.execSync` + tmpdir에 mock sincenety 바이너리 주입. stdin JSON 파싱, Bash 외 도구 패스스루, MISSING→exit 2 차단, OK→통과, verify 자체 실패 시 무소음 통과 |

### 4-2. 신규 E2E 테스트 파일 (2개)

| 파일 | 테스트 수 | 대상 | 테스트 기법 |
|------|----------|------|-----------|
| `tests/e2e/email-render.test.ts` | 12 | 이메일 HTML 렌더링 전체 흐름 | 실제 SqlJsAdapter + `renderDailyEmail` 호출. daily/weekly/monthly 렌더링, AI 배지, 가중치별 축약, 크로스 디바이스 머지, 빈 데이터 처리 |
| `tests/e2e/email-ethereal.test.ts` | 1 (skip) | 실제 SMTP 발송 | Ethereal 테스트 메일 서버. `ETHEREAL_TEST=1` env 없으면 자동 skip. 실제 nodemailer 발송 + 수신 확인 |

### 4-3. 확장된 기존 테스트 파일 (3개)

| 파일 | 추가 수 | 추가 내용 |
|------|---------|----------|
| `tests/out.test.ts` | +11 | `printVerifyTable` (6개: 헤더, OK/MISSING/STALE/EMAILED 마크, 빈 배열), `findUnsentReports` (5개: 미발송 weekly/monthly, 발송 완료 스킵, 월초 경계 주차 계산) |
| `tests/circle.test.ts` | +6 | `runCircle — rerun` (6개: invalidate+changedDates 병합, 발송본 스킵 경고, 존재하지 않는 날짜, 혼합 날짜, smart 모드 조합, 중복 제거) |
| `tests/e2e/email-render.test.ts` | +5 | AI 배지 렌더링, provider 미설정 시 동작, low/high weight 렌더링 분기, `resolveAiProvider` catch 경로 |

### 4-4. 테스트 공통 헬퍼

| 파일 | 설명 |
|------|------|
| `tests/helpers.ts` | 공유 테스트 유틸. `createTestAdapter` (tmpdir + SqlJsAdapter 초기화), `cleanupTestAdapter` (정리), `makeDailyReport`/`makeGatherReport` 팩토리 함수 |

---

## 5. 테스트 계획/결과 문서

| 파일 | 설명 |
|------|------|
| `docs/test-plan-v0.8.7-20260416.md` | 96개 신규 테스트의 상세 계획. 대상 함수, 입력/기대값, mock 전략, 병렬 실행 구간, 횡단 관심사(fake timer, process.exit mock 등) |
| `docs/test-results-v0.8.7-20260416.md` | 실행 결과. 276/276 통과, 파일별 소요시간, 실행 중 발견된 3개 에러와 수정, 5개 주요 발견사항 (`emailedAt=0` 비대칭 등), 개선 제안(P0/P1/P2), 커버리지 매트릭스 |
| `docs/improvement-plan-v0.8.7-20260416.md` | v0.8.7 개선 계획 (9개 기능 구현 방안) |

---

## 6. README 변경

| 파일 | 변경 내용 |
|------|----------|
| `README.md` | v0.8.7 changelog 추가 (영문) — 버그 설명, 근본 원인, 수정 내용, 변경 파일 |
| `README.ko.md` | v0.8.7 changelog 추가 (한글) — 동일 내용 한국어 버전 |

---

## 7. 테스트 실행 최종 결과

```
Test Files  17 passed | 1 skipped (18)
Tests       276 passed | 1 skipped (277)
Duration    1.57s
```

| 구분 | 수 |
|------|---|
| 기존 테스트 | 180 |
| 신규 테스트 | 96 |
| 스킵 | 1 (email-ethereal — `ETHEREAL_TEST` env 필요) |
| 실패 | 0 |

### 테스트 기법 요약

| 기법 | 사용 파일 | 설명 |
|------|----------|------|
| Integration (실제 DB) | freshness, circle, e2e/email-render | SqlJsAdapter + tmpdir로 실제 DB 생성/조회 |
| Mock storage | ai-provider, doctor, out, weights | `vi.fn()` 기반 mock 객체로 DB 의존 제거 |
| Fake timers | doctor | `vi.useFakeTimers()`로 날짜 고정 (2026-04-16) |
| Process mock | ai-provider | `process.exit`/`process.stdout.write` spy |
| Shell script E2E | guard-hook | `execSync` + tmpdir mock 바이너리 + PATH 주입 |
| Console spy | circle (rerun), doctor | `vi.spyOn(console, 'log'/'warn')` 출력 검증 |
| Proxy wrapper | circle (summaryErrors) | `new Proxy(storage, {...})`로 특정 메서드만 throw |
| Env var mock | ai-provider | `process.env.ANTHROPIC_API_KEY` 임시 설정/복원 |
